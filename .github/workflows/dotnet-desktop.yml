name: Build & Publish

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Build & Pack
        run: dotnet pack HexBox.WinUI/HexBox.WinUI.csproj -c Release

      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: nupkg
          path: HexBox.WinUI/bin/Release/*.nupkg

  publish:
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: windows-latest

    permissions:
      packages: write

    steps:
      - name: Download package
        uses: actions/download-artifact@v4
        with:
          name: nupkg

      - name: Push to GitHub Packages
        run: dotnet nuget push *.nupkg --source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json --api-key ${{ secrets.GITHUB_TOKEN }}
